struct Foo {
  int a;
  int b;
};

struct Bar {
  int a;
};

int x;

struct Foo f;
struct Bar b;

int foo (int bar)
{
  
  int a;
  a = f.b;
  if (bar < 1)
    bar = bar + 1;
  else
    bar = bar + 5;
  
  while (bar < 0) {
    x = x + 1;
  }
  
  x = x + 2;
  printf("%i", bar);
  return 0;
}

x