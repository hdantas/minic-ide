struct inner{int f; int farray[5];};

struct inner globalinstance;
int globalarray[5];
int global;

int foo(int x) {
  struct inner localinstance;
  
  int localarray[5];
  int local;
  
  local = 4;
  
  /* WORK
  globalinstance.f = local;
  globalinstance.f = global;
  globalinstance.f = globalarray[1];
  globalinstance.f = localarray[2];
  localinstance.f = local;
  localinstance.f = global;
  localinstance.f = globalarray[1];
  localinstance.f = localarray[2];

  globalinstance.farray[1] = local;
  globalinstance.farray[1] = global;
  globalinstance.farray[1] = globalarray[1];
  globalinstance.farray[1] = localarray[2];
  globalinstance.farray = globalarray;
  globalinstance.farray = localarray;
  localinstance.farray[1] = local;
  localinstance.farray[1] = global;
  localinstance.farray[1] = globalarray[1];
  localinstance.farray[1] = localarray[2];
  localinstance.farray = globalarray;
  localinstance.farray = localarray;
  */
  localinstance.f = local;
    local = localinstance.f;
 //    local = localinstance.farray[1];
	// local = globalinstance.f;
 //    local = globalinstance.farray[1];
 //    
	// global = localinstance.f;
 //    global = localinstance.farray[1];
	// global = globalinstance.f;
 //    global = globalinstance.farray[1];
 //    
 //    localarray[1] = local;
 //    localarray[1] = global;
 //    localarray = localinstance.farray;
 //    localarray = globalinstance.farray;
 //    
 //    globalarray[1] = local;
 //    globalarray = localinstance.farray;
 //    globalarray = localinstance.farray;
 //    globalarray = globalinstance.farray;


  // int returnvalue;
  // returnvalue = local + global + globalinstance.f + globalinstance.farray[3] + localinstance.f + localinstance.farray[3];
  return 0;
}

foo(1)